
{
    int n, a, b, c, i;
    input(n);
    a := 1;
    b := 1;
    i := 3;
    if (n == 0) {
        print(0);
    };
    else {
        if (n == 1 or n == 2) {
            print(1);
        };
        else {
             while (i <= n) {
                c := b;
                b := a + b;
                a := c;
                i := i + 1;
             };
             print(b);
        };
    };
}
